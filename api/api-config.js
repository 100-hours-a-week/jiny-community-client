// API 기본 설정
const API_CONFIG = {
  BASE_URL: 'http://localhost:8080', // 실제 백엔드 서버 주소로 변경 필요
  TIMEOUT: 10000,
  DEFAULT_HEADERS: {
    Accept: 'application/json',
  },
};

export default API_CONFIG;
